<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="LightText">
        <item name="android:textColor">#ffffff</item>
    </style>

    <style name="LightTextCaption" parent="LightText">
        <item name="android:textSize">16sp</item>
        <item name="android:gravity">bottom</item>
    </style>

    <style name="LightBoldText" parent="LightText">
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">18sp</item>
    </style>

    <style name="headerFields">
        <item name="android:paddingLeft">20dp</item>
    </style>

    <style name="LightBoldCaption" parent="LightBoldText">
        <item name="android:textSize">30sp</item>
    </style>

    <style name="LightBoldDescription" parent="LightBoldText">
        <item name="android:textSize">12sp</item>
    </style>

</resources>
